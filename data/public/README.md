# Public data release

This directory is intentionally empty at first publication. Add `corpus_metadata_public.csv` only after generating it from a private workbook and completing the review steps in `PUBLISH_MANIFEST.md`.

The public CSV is metadata-only. It must not contain full text, scans, long quotations, session-bound URLs, or access credentials.
